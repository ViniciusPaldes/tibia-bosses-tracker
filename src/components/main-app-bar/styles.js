import { makeStyles } from "@material-ui/core";

export const useStyles = makeStyles((theme) => ({
    appBar: {},
    title: {
      flexGrow: 1,
      fontWeight: "bold",
    },
    badge: {
      top: 5,
      right: 5,
    },
  
    badgeContent: {
      backgroundColor: "#C06605",
      fontWeight: "bold",
    },
  }));
  