import CancelIcon from '@material-ui/icons/Cancel';
import React from 'react';

const WhiteDeleteIcon = (props) => {
  return <CancelIcon {...props} style={{ color: 'white' }} />;
};

export default WhiteDeleteIcon;
